#!/usr/bin/env node
const chalk = require('chalk');
const yel = chalk.yellow.bind(chalk);
const red = chalk.red.bind(chalk);
const bol = chalk.bol.bind(chalk);

console.log(`

${yel(`*********************************************************`)}
${yel(`**                                                     **`)}
${yel(`**                                                     **`)}
${yel(`**               ${bol(`Ryan Stevens`)}                                      **`)}
${yel(`**                                                     **`)}
${yel(`**                                                     **`)}
${yel(`*********************************************************`)}




`);